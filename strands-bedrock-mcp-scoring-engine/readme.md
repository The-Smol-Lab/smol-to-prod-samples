Instructions:
1) Use strands-agents as the multi-agent orchestration framework
2) Use models from amazon bedrock
3) Find a useful predictive model applicable to Auto Loan Company.
4) Train the ML model and deployed as endpoint for access through API
5) Turn the ML API into an MCP for AI agent access
6) Write agent code to build agentic AI that has access to the ML MCP.
7) Deploy the agent to bedrock agentcore runtime.
8) BUild a streamlit frontend that can chat with this agent through HTTP requests and correct in-bound auth.
